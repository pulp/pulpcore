Consolidated all calls to ```hashlib.new` to use the new, ``pulpcore.app.pulp_hashlib`` module. This
module ensures that only allowed hashers are instantiated and provides one place for that check to
occur.

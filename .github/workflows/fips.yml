# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulpcore' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template
---
name: Pulp FIPS CI
on:
  workflow_dispatch:

jobs:
  vagrant:
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - test_type: pulp3-source-centos8-fips
          - test_type: pulp3-source-centos7-fips
    steps:
      - uses: actions/checkout@v2.3.1
        with:
          submodules: true
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.7
      - name: Ansible
        run: |
          pip3 install -U pip
          pip3 install "ansible>2.9,<3"
          ansible --version
      - name: Pulp Installer
        run: |
          cd ..
          git clone --depth=1 https://github.com/pulp/pulp_installer
      - name: Install Vagrant
        shell: bash
        run: |
          cd ../pulp_installer/
          git submodule update --init
          .github/workflows/scripts/install.sh ${{ matrix.test_type }}
          cd ..
          git clone https://github.com/pulp/pulp_file
      - name: Run Vagrant
        shell: bash
        run: |
          cd ../pulp_installer
          sed -e "/pulp_source_dir/a pulp_webserver_disable_https: true" example.dev-config.yml > local.dev-config.yml
          echo "vagrant ssh \$1 -c \"workon pulp && cd devel/pulpcore && pip install -r functest_requirements.txt\"" >> .github/workflows/scripts/script.sh
          echo "vagrant ssh \$1 -c \"workon pulp && cd devel/pulpcore/ && pytest -v -r sx --color=yes --pyargs pulpcore.tests.functional\"" >> .github/workflows/scripts/script.sh
          .github/workflows/scripts/script.sh ${{ matrix.test_type }}
        env:
          PY_COLORS: '1'
          ANSIBLE_FORCE_COLOR: '1'
      - name: After failure
        if: failure()
        run: |
          cd ../pulp_installer
          .github/workflows/scripts/after_failure.sh ${{ matrix.test_type }}
